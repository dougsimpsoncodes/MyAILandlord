# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=changeme
EXPO_PUBLIC_SUPABASE_ANON_KEY=changeme
EXPO_PUBLIC_SUPABASE_FUNCTIONS_URL=changeme

# Legacy (no longer used)
# Note: GEMINI_API_KEY is now stored server-side in Supabase secrets
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=changeme

# iOS MVP focus flag (gates optional platform-specific behavior)
EXPO_PUBLIC_IOS_MVP=0

# Deep-link centralization toggle (Option B-lite)
EXPO_PUBLIC_LINKING_REFACTOR=0

# Experimental clean app entry (off by default)
EXPO_PUBLIC_EXPERIMENTAL_APP=0
